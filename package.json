{
  "name": "magento2-admin-mcp-server",
  "version": "3.0.0",
  "description": "MCP Server for Magento 2 Admin REST API",
  "scripts": {
    "start": "bun src/server.ts",
    "build": "bun build src/server.ts --outdir ./dist --target bun && cp -r src/templates dist/",
    "dev": "LOG_LEVEL=debug bun src/server.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Neutrome Labs",
  "license": "ISC",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.25.1",
    "axios": "^1.9.0",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "zod": "^4.2.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.1",
    "typescript": "^5.5.4"
  }
}
